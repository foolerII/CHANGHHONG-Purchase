# 预计返利管理原型：常驻协作指令

## 事实优先

- 页面字段、接口、权限和状态仅可来自 `docs/00-facts.md` 或新增的可追溯证据。
- 不得凭空新增后端字段；演示字段必须使用现有接口/页面中已经出现的字段名。
- 任何暂未在代码中确认的设想须标记为“待确认”，不得伪装为既有规则。

## 技术与命名

- 原型为零依赖静态 HTML，双击 `prototype/index.html` 即可打开。
- 原型数据集中放在 `prototype/mock-data.js`，使用 lowerCamelCase，字段名与当前 Vue 页面/API 一致。
- 保留品牌素材来源；`refs/` 是只读事实摘录，只可新增来源文件（如 `refs/backend.md`），不可改写已有结论。
- 金额一律按「分」做整数运算后再展示，禁止直接对小数做加减。

## 更新顺序

1. 先更新 `docs/00-facts.md`（如发现新事实），后端事实同步补入 `refs/backend.md`。
2. 再更新 PRD、状态文档和原型。
3. 在 `docs/CHANGELOG.md` 记录改动、原因和影响范围。
